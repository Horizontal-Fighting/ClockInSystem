# 国内镜像，加速依赖安装。CI/服务器环境若需官方源，用 npm config set registry 覆盖即可。
registry=https://registry.npmmirror.com

# Prisma 引擎二进制同样走镜像（否则下载 engines 会卡住）
PRISMA_DOWNLOAD_HOST=https://registry.npmmirror.com/-/binary/prisma

# 钉钉/私有包场景可在此加 @scope:registry，本项目暂无
